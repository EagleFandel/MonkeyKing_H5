# 悟空哆哆 H5 Design V2 · 修复优先计划

## 1. 修复顺序
1. 基础层：token、排版、动效变量、全局样式。
2. 契约层：类型定义、路由 meta、组件 props 升级。
3. 组件层：`CandidateCard`、`DodoAssistantPanel`、`PaymentSheet`、`StatusState`。
4. 页面层：首页 → 四场景 → 支付/消息/个人中心 → 认证/投诉/详情。
5. 回归层：单测/E2E/可访问性检查。

## 2. 工作包

### WP-A 基础设计系统
- 目标：完成全局 token、动效、焦点可见性。
- 输出：`src/style.css`、`src/types/ui.ts`、`src/types/design.ts`。

### WP-B 组件契约升级
- 目标：核心组件支持变体与状态。
- 输出：三大核心组件 + 新状态组件。

### WP-C 页面重构
- 目标：全站五态覆盖与 IA 强化。
- 输出：首页与所有关键业务页改造。

### WP-D 自动化回归
- 目标：确保改造不破业务链路。
- 输出：新增单测与 E2E 场景，全部通过。

## 3. 完成判定
- P0 全部清零。
- P1 最多保留 3 项且需书面豁免。
- 审计评分达到 `>= 90`。

